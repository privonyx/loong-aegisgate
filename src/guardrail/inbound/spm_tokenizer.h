#pragma once

// Guard-facing include path for the SentencePiece tokenizer used by
// GuardClassifier. The implementation lives with the existing tokenizer module
// to keep tokenization primitives in one place.
#include "cache/tokenizer.h"
